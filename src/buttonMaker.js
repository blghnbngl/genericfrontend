import React from 'react'

const ButtonMaker = ({ buttonName }) => {
    return (
        <div>
            <button>
                {buttonName}
            </button>
        </div>
    )
}

export default ButtonMaker